export const SVG_ANIMATION_PROMPT =
  'SVG resources (image/svg+xml) may contain SMIL or CSS animation. They are not raster Reference Images and must not be sent to image-analysis APIs. Composer SVG uploads belong to animationEffects; preserve their actual slot ownership, set that resource to 用户上传 and describe the exact filename and intended visual role in treatment (including a background role). For SVG uploaded directly to backgroundBoard, tileFaces or endCard, keep that slot. Do not infer the role from the filename when the request already states it. For requested procedural vector animation without uploads, let the build author SVG animation as code using 内置默认 with an explicit treatment; raster image generation cannot produce SVG animation. During builds, read references/svg-animation.md and use assets/starter/work/svg-animation.mjs for a decorative animated background. Keep SVG in an isolated img image context with an embedded data URL, never inject uploaded markup into the document or execute its scripts. Preserve declarative animation, original colors and geometry. Canvas drawImage/createPattern and WebGL textures do not preserve image animation; do not use them for an animated SVG. Remove the old background drawing and initialize the foreground canvas with alpha enabled, clearing it transparently each frame, so the background remains visible. Keep gameplay/UI above the SVG and pointer input on the game. Fit with cover without stretching, respond to portrait/landscape resizing, and use separate orientation assets only when supplied or requested. Retain one source without restarting animation on resize when possible. Test actual animation over time, portrait and landscape including rotation, gameplay input and offline loading in the final HTML when browser acceptance is enabled; do not claim runtime verification when it is disabled.'
